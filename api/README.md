this is a simple api using bun's native http server serving the playwright a tool that screenshots the websites, the lib dir contains the screenshot logic,
the server serves on port 3000, make sure the port is not busy, just run:
```javascript
bun run start
```
or
```javascript
npm run start
```
